// client/vite.config.ts (ASSUMED CONTENT - DELETE LINES MARKED WITH ❌)

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
});
